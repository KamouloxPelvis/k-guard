server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Crucial pour les SPA (React/Vue/Vite) :
        try_files $uri $uri/ /index.html;
    }
}