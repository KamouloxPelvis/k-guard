apiVersion: apps/v1
kind: Deployment
metadata:
  name: kguard-deployment
  namespace: k-guard
spec:
  replicas: 1
  strategy:
    type: Recreate 
  selector:
    matchLabels:
      app: k-guard
  template:
    metadata:
      labels:
        app: k-guard
    spec:
      serviceAccountName: k-guard-sa
      automountServiceAccountToken: true
      imagePullSecrets:
        - name: regcred

      securityContext:
        fsGroup: 1000
        supplementalGroups: [988] 

      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: trivy-cache
          persistentVolumeClaim:
            claimName: trivy-cache-pvc # On utilise ton PVC existant
      
      initContainers:
      - name: init-permissions
        image: busybox
        # On s'assure que l'utilisateur 1000 a les droits sur tout le volume
        command: ["sh", "-c", "chown -R 1000:1000 /data/trivy-cache"]
        volumeMounts:
        - name: trivy-cache
          mountPath: /data/trivy-cache

      containers:
      - name: kguard-app
        image: registry.gitlab.com/portfolio-kamal-guidadou/k-guard/kguard-app:latest
        securityContext:
          runAsUser: 1000
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8000
        volumeMounts:
          - mountPath: /var/run/docker.sock
            name: docker-socket
          - mountPath: /data/trivy-cache
            name: trivy-cache
          # ON MONTE LE MÊME PVC ICI POUR LA DB (via un sous-répertoire)
          - mountPath: /app/backend/data
            name: trivy-cache
            subPath: database 
        envFrom:
          - secretRef:
              name: k-guard-secrets
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 45
          periodSeconds: 30
        resources:
          limits:
            cpu: "1.5"
            memory: "2Gi"
          requests:
            cpu: "500m"
            memory: "1Gi"