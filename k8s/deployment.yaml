apiVersion: apps/v1
kind: Deployment
metadata:
  name: k-guard-deployment
  namespace: k-guard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: k-guard
  template:
    metadata:
      labels:
        app: k-guard
    spec:
      serviceAccountName: k-guard-sa

      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        supplementalGroups: [988]

      volumes:
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: trivy-cache
          persistentVolumeClaim:
            claimName: trivy-cache-pvc

      containers:
      # --- 1. BACKEND (FastAPI + Trivy) ---
      - name: backend
        image: k-guard-backend:latest
        securityContext:
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 8000
        volumeMounts:
          - mountPath: /var/run/docker.sock
            name: docker-socket
          - mountPath: /data/trivy-cache
            name: trivy-cache

        envFrom:
          - secretRef:
              name: k-guard-secrets

        # Check de santé (Liveness)
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 45
          periodSeconds: 30      
          failureThreshold: 5

        # Limites de ressources
        resources:
          limits:
            cpu: "1.5"        # On lui donne 1 coeur et demi pour le scan
            memory: "2Gi"     # On monte à 2Go car Trivy est gourmand en DB
          requests:
            cpu: "500m"       # On assure un minimum de puissance pour l'API
            memory: "1Gi"     # On garantit 1Go pour être serein au repos

      # --- 2. FRONTEND (Vue.js + Nginx) ---
      - name: frontend
        image: k-guard-frontend:latest
        ports:
        - containerPort: 8080 
        securityContext:
          allowPrivilegeEscalation: false
        resources:
          limits:
            cpu: "200m"     
            memory: "1024Mi" 
          requests:
            cpu: "200m"
            memory: "512Mi"